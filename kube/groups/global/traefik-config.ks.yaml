# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: traefik-config
  namespace: bcl
spec:
  interval: 1h
  path: ./kube/apps/traefik/traefik-config
  prune: true
  sourceRef:
    kind: GitRepository
    name: bcl
  wait: true
