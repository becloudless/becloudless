# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: gitea
  namespace: bcl
spec:
  interval: 10m
  timeout: 1m
  path: ./kube/apps/gitea
  prune: false # could be hosting the repo itself
  sourceRef:
    kind: GitRepository
    name: bcl
  wait: true
  postBuild:
    substituteFrom:
      - name: global-env
        kind: ConfigMap
