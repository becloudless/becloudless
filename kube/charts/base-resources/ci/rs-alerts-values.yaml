# yaml-language-server: $schema=../schema/values.schema.json

resources:
  alerts:
    main:
      eventSeverity: info
      eventSources:
        - kind: GitRepository
          name: '*'
      providerRef:
        name: slack
