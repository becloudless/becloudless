# yaml-language-server: $schema=../schema/values.schema.json

resources:
  verticalPodAutoscalers:
    main:
      targetRef:
        apiVersion: apps/v1
        kind: Deployment
        name: main
      updatePolicy:
        updateMode: Auto
