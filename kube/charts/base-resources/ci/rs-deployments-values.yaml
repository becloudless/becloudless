# yaml-language-server: $schema=../schema/values.schema.json

resources:
  deployments:
    main:
      replicas: 2
      selector:
        matchLabels:
          app: myapp
      template:
        metadata:
          labels:
            app: myapp
        spec:
          containers:
            - name: myapp
              image: myimage:latest
