# yaml-language-server: $schema=../schema/values.schema.json

resources:
  roles:
    main:
      rules:
        - apiGroups: [""]
          resources: ["pods"]
          verbs: ["get", "list", "watch"]
