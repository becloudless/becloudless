# yaml-language-server: $schema=../schema/values.schema.json

resources:
  clusterRoles:
    main:
      rules:
        - apiGroups: [""]
          resources: ["nodes"]
          verbs: ["get", "list", "watch"]
