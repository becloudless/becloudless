# yaml-language-server: $schema=../values.schema.json

resources:
  persistentVolumeClaims:
    main:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 1Gi
