---
# Source: resources-test/templates/loader.yaml
apiVersion: notification.toolkit.fluxcd.io/v1
kind: Receiver
metadata:
  name: test-release
  namespace: default
spec:
  events:
  - ping
  - push
  resources:
  - kind: GitRepository
    name: infra
  secretRef:
    name: infra-webhook
  type: github
