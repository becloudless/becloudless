# yaml-language-server: $schema=../schema/values.schema.json

resources:
  daemonSets:
    main:
      selector:
        matchLabels:
          app: myapp
      template:
        metadata:
          labels:
            app: myapp
        spec:
          containers:
            - name: myapp
              image: myimage:latest
