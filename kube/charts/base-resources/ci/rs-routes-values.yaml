# yaml-language-server: $schema=../schema/values.schema.json

resources:
  routes:
    main:
      host: myapp.example.com
      to:
        kind: Service
        name: myapp
      port:
        targetPort: http
