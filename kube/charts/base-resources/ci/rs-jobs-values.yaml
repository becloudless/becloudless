# yaml-language-server: $schema=../values.schema.json

resources:
  jobs:
    main:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: myapp
              image: myimage:latest
