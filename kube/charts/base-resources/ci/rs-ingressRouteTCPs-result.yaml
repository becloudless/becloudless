---
# Source: base-resources-test/templates/loader.yaml
apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: test-release
  namespace: default
spec:
  entryPoints:
  - websecure
  routes:
  - match: HostSNI(`ssh-gitea.example.com`)
    services:
    - name: gitea-ssh
      port: 22
  tls:
    passthrough: false
