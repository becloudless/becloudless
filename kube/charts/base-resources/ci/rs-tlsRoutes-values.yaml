# yaml-language-server: $schema=../values.schema.json

resources:
  tlsRoutes:
    main:
      parentRefs:
        - name: main-gateway
      hostnames:
        - example.com
      rules:
        - backendRefs:
            - name: main-service
              port: 443
