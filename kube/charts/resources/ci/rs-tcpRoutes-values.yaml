# yaml-language-server: $schema=../values.schema.json

resources:
  tcpRoutes:
    main:
      parentRefs:
        - name: main-gateway
      rules:
        - backendRefs:
            - name: main-service
              port: 3306
