resources:
  configMaps:
    labels:
      app: genre
      truc: bidule
    toto:
      data:
        something: interesting
      binaryData:
        blob: aGVsbG8=
  secrets:
    labels:
      app: genre2
      truc: bidule2
    titi:
      stringData:
        something: interesting
  horizontalPodAutoscalers:
    myhpa:
      spec:
        scaleTargetRef:
          apiVersion: apps/v1
          kind: Deployment
          name: myapp
        minReplicas: 1
        maxReplicas: 3
  services:
    myservice:
      spec:
        selector:
          app: myapp
        ports:
          - port: 80
            targetPort: 8080
