# yaml-language-server: $schema=../values.schema.json

resources:
  serviceMonitors:
    main:
      selector:
        matchLabels:
          app: main
      endpoints:
        - port: metrics
          interval: 30s
