---
# Source: resources-test/templates/loader.yaml
apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: test-release-br0-dhcp
  namespace: default
spec:
  config: |
    {
      "cniVersion": "0.3.0",
      "name": "mymacvlan",
      "plugins": [
        {
          "type": "macvlan",
          "master": "br0",
          "mode": "bridge",
          "ipam": {
            "type": "dhcp",
            "request": [
              {
                "skipDefault": true,
                "option": "subnet-mask"
              }
            ]
          }
        }
      ]
    }
