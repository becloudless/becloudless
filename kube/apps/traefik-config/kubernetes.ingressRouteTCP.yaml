---
apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: kubernetes
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
  - match: HostSNI(`kube.${BCL_GLOBAL_DOMAIN}`)
    services:
    - name: kubernetes
      port: 443
  tls:
    passthrough: true
