apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - https://raw.githubusercontent.com/k8snetworkplumbingwg/multus-cni/v4.2.4/deployments/multus-daemonset.yml
patches:
  # multus OOM without a limit
  - path: memory-limit.patch.yaml
    target:
      kind: DaemonSet
      name: kube-multus-ds
