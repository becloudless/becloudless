apiVersion: traefik.io/v1alpha1
kind: IngressRouteTCP
metadata:
  name: lldap-ldaps
  namespace: lldap
spec:
  entryPoints:
    - websecure
  routes:
    - match: HostSNI(`lldaps.${BCL_GLOBAL_DOMAIN}`)
      services:
        - name: lldap
          port: 6360
  tls:
    passthrough: true
