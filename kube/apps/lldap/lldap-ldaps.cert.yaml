apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: tls-lldaps
  namespace: lldap
spec:
  secretName: tls-lldaps
  dnsNames:
    - lldaps.${BCL_GLOBAL_DOMAIN}
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer

